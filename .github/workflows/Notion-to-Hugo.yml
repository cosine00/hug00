name: Notion to Hugo with R2 Sync

on:
  schedule:
    - cron: "0 */4 * * *" # 每 4 小时执行一次
  workflow_dispatch:      # 允许手动触发
  repository_dispatch:
    types: 
      - notion_sync

permissions:
  contents: write

jobs:
  build_and_sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 1. 将 Notion 转为 Markdown，输出到临时目录
      - name: Convert Notion to Markdown (to Temp Dir)
        uses: cosine00/notion2markdown-action@v1
        with:
          notion_secret: ${{ secrets.NOTION_SECRET }}
          database_id: ${{ secrets.NOTION_DATABASE_ID }}
          pic_migrate: false
          output_page_dir: 'temp_content'
          output_post_dir: 'temp_content/posts/notion'
          clean_unpublished_post: true
          metas_keeped: slug, date, author, tags, draft, created, updated, ptype

      # 2. 准备 Python 环境
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      # 3. 安装依赖
      - name: Install Python Dependencies
        run: |
          pip install boto3 requests Pillow

      # 4. 执行脚本：处理图片、时间格式化、重命名
      - name: Process Images and Format Posts
        env:
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
          R2_PUBLIC_DOMAIN: ${{ secrets.R2_PUBLIC_DOMAIN }}
        run: |
          python .github/scripts/upload_r2.py

      # 5. 合并处理好的文章到正式目录
      - name: Merge Updates to Content
        run: |
          mkdir -p content/posts/notion
          if [ -d "temp_content/posts/notion" ]; then
            cp -rf temp_content/posts/notion/* content/posts/notion/
          fi

      # 6. 推送更新
      - name: Commit and Push Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          file_pattern: 'content/'
          commit_message: "Sync posts from Notion and upload images to R2"
          commit_user_name: "GitHub Action"
          commit_user_email: "action@github.com"